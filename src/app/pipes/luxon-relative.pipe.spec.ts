import { LuxonRelativePipe } from './luxon-relative.pipe';

describe('LuxonRelativePipe', () => {
  it('create an instance', () => {
    const pipe = new LuxonRelativePipe();
    expect(pipe).toBeTruthy();
  });
});
