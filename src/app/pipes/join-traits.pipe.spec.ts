import { JoinTraitsPipe } from './join-traits.pipe';

describe('JoinTraitsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinTraitsPipe();
    expect(pipe).toBeTruthy();
  });
});
