import { JoinSymbolsPipe } from './join-deposits.pipe';

describe('JoinSymbolsPipe', () => {
  it('create an instance', () => {
    const pipe = new JoinSymbolsPipe();
    expect(pipe).toBeTruthy();
  });
});
